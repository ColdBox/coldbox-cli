# ColdBox Application - AI Instructions

> **Project Language**: |LANGUAGE|

## Guidelines

This project uses ColdBox framework with the following guidelines available in `.ai/guidelines/`:

- **Core Framework**: ColdBox HMVC conventions
- **Language**: |LANGUAGE| syntax and features
- **Testing**: TestBox BDD/TDD patterns
- **Dependency Injection**: WireBox patterns
- **Installed Modules**: Guidelines for all installed modules

**Always consult guidelines before generating code** to ensure framework conventions are followed.

## Skills

Implementation patterns are available in `.ai/skills/`. When implementing features, request the relevant skill:

- Handler development
- REST API creation
- Testing patterns
- Security implementation
- ORM operations
- And more...

**Load skills on-demand** when you need specific implementation guidance.

## Code Generation Standards

### BoxLang Syntax (Primary)
```boxlang
class UserHandler extends coldbox.system.EventHandler {
    property name="userService" inject;

    function index( event, rc, prc ) {
        prc.users = userService.getAll()
        event.setView( "users/index" )
    }
}
```

### Key Conventions
- Use `class` syntax for BoxLang
- Properties with `inject` for dependency injection
- No semicolons except property declarations
- Actions are functions in handlers
- Use event.renderData() for REST responses

### Testing
```boxlang
describe( "User Handler", function(){
    it( "should list all users", function(){
        var event = execute( event="users.index", renderResults=true )
        expect( event.getValue( "users", "" ) ).toBeArray()
    } )
} )
```

## Project Structure

```
/config          - Application configuration
/handlers        - Request handlers (controllers)
/models          - Business logic and services
/views           - View templates
/layouts         - Layout wrappers
/tests           - TestBox test suites
/.ai             - AI guidelines and skills
```

## Workflow

1. **Understand Requirements**: Review what needs to be built
2. **Consult Guidelines**: Check `.ai/guidelines/` for framework conventions
3. **Load Skills**: Request relevant skill from `.ai/skills/` for implementation pattern
4. **Generate Code**: Follow guidelines + skill pattern
5. **Write Tests**: Always include TestBox tests
6. **Verify**: Ensure code follows ColdBox conventions

---

*This file was auto-generated by ColdBox CLI AI integration. Run `coldbox ai refresh` to update.*
